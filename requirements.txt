# Updated requirements to resolve dependency conflicts
# Note: These are base requirements. Kaggle pre-installs torch with CUDA support.

# Core Scientific Stack
numpy>=1.26.0,<2.0  # Compatible with most packages
matplotlib
pandas
scipy

# Filesystem & Data (Kaggle-compatible)
fsspec==2025.3.0  # FIXED: Exact version required by gcsfs on Kaggle
pyarrow>=14.0.0,<20.0.0  # FIXED: Compatible with cudf-cu12 (Kaggle GPU DataFrames)
huggingface-hub<1.0,>=0.30.0  # Compatible with transformers

# Utilities
tqdm
click>=7.0,!=8.3.0  # FIXED: Compatible with ray 2.51.1
rich>=12.4.4,<14  # FIXED: Compatible with bigframes

# Machine Learning
torch>=2.0.0  # Kaggle has torch 2.6.0+cu124 pre-installed
torchvision
# CRITICAL FIX: Downgraded datasets to maintain compatibility with pyarrow<20 (Kaggle constraint)
# datasets>=4.4.0 requires pyarrow>=21.0.0 which conflicts with cudf-cu12
datasets>=2.14.0,<3.0.0
optuna
mlflow

# Visualization
plotly
kaleido

# Legacy/Compatibility
protobuf>=3.20.3,<4.0.0  # FIXED: TensorFlow requires >=3.20.3, but <6.0 causes issues
torchtext>=0.14.0
portalocker>=2.0.0
medpy>=0.4.0
pydantic>=2.0,<2.12  # Compatible with gradio
cryptography>=19.0,<44  # FIXED: Compatible with pydrive2
pyOpenSSL>=19.1.0,<=24.2.1  # FIXED: Compatible with pydrive2

# Testing
pytest
