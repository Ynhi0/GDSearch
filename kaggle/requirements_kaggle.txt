# Kaggle Requirements
# Most packages already available in Kaggle environment

# Core packages (usually pre-installed)
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
tqdm>=4.65.0

# Additional (if needed)
# matplotlib>=3.7.0
# pandas>=2.0.0
